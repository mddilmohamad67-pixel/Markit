# Environment variables (example)
MONGO_URI=mongodb://localhost:27017/markit
PORT=4000
NEXT_PUBLIC_API_URL=http://localhost:4000
# FIREBASE_SERVICE_ACCOUNT_JSON should contain the JSON string of the service account for firebase-admin initialization
# e.g. FIREBASE_SERVICE_ACCOUNT_JSON="{...}"
